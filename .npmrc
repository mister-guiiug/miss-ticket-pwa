@mister-guiiug:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

# react-qr-reader@3.0.0-beta-1 déclare un peer react 16/17 (abandonné, incompatible
# React 19). On tolère le conflit jusqu'à migration vers un scanner QR maintenu.
legacy-peer-deps=true
